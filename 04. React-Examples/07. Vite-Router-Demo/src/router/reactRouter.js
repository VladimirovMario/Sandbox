// Centralized react-router re-exports.
// Only add imports here when used in the app.
import {
    createBrowserRouter,
    RouterProvider,
    Outlet,
    Link,
    NavLink,
} from 'react-router';

export { createBrowserRouter, RouterProvider, Outlet, Link, NavLink };
