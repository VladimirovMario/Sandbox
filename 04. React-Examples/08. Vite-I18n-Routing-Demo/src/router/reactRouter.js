// Centralized react-router re-exports.
// Only add imports here when used in the app.
import {
    createBrowserRouter,
    RouterProvider,
    Outlet,
    Link,
    NavLink,
    useNavigation,
    useParams,
    useNavigate,
    useLocation,
} from 'react-router';

export {
    createBrowserRouter,
    RouterProvider,
    Outlet,
    Link,
    NavLink,
    useNavigation,
    useParams,
    useNavigate,
    useLocation,
};
